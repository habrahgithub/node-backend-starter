# Storage Policy Skill

Purpose: prevent repo bloat and keep storage compliant.

Steps:
- Follow `docs/STORAGE.md` for what can be committed.
- Never add logs, dumps, caches, or build outputs.
- If a generated file is necessary, confirm before committing.
- Update `.gitignore` when new artifact patterns appear.
