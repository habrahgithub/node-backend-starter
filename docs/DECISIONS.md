# Decisions

Record decisions that affect repo behavior, structure, or policy.

Template:
- YYYY-MM-DD: Decision statement. Rationale.

Entries:
- 2026-03-08: Added explicit Axis directive-set binding in `projects/SWD-ARC/apps/controls-center/app/api/_gateway/mcpGateway.js` via `AXIS_ALLOWED_DIRECTIVE_IDS` (with fail-closed default to `AXIS_ACTIVE_DIRECTIVE_ID`) so governed MCP access can be re-bound to `ARC-SDLC-01` without disabling directive checks.
- 2026-03-07: Added optional persistent Axis directory grants for `projects/swd-landing` and `projects/SWD-ARC/apps/controls-center`, backed by env-configured grant-store files with fail-closed load/save behavior, TTL pruning, and optional approval-token gating for `directory_ux` mutations while preserving preflight hard-gate enforcement for host-workspace reads.
- 2026-03-07: Added deterministic directory-grant TTL enforcement for `axis.context_access.directory_ux` in `projects/swd-landing`; granted roots now carry `expires_at`/`ttl_seconds`, expired grants are pruned fail-closed, and read/list denies can return `grant_expired` when an expired grant would otherwise match.
- 2026-03-07: Added Desktop-Commander-style directory permission UX to SWD landing MCP via `axis.context_access.directory_ux` (`status|list|grant|revoke|clear`) with directive validation, scoped grant root (`AXIS_GRANTABLE_ROOT`, default `/home/habib/workspace`), and bounded in-memory grant list for read/list path expansion.
- 2026-03-07: Added `AXIS-CONTEXT-ACCESS-0002` lightweight hard-gate regression suite (`projects/swd-landing/scripts/axis-hard-gate-regression.mjs`) covering allow + deny matrix (`preflight_missing|preflight_failed|preflight_expired|scope_ambiguous`) with non-zero exit on regression and local-only runtime dependency.
- 2026-03-07: Added Axis host-workspace hard gate in `projects/swd-landing` so `axis.context_access.read` requires a cached local preflight (`axis-profile-switch.sh local`) with `STATUS=PASS` and 5-minute TTL; deterministic denies now include `preflight_missing|preflight_expired|preflight_failed|scope_ambiguous`.
- 2026-03-07: Standardized Axis host-workspace access preflight to require `projects/swd-landing/scripts/axis-profile-switch.sh local` before any workspace-read task, with explicit local/remote profile separation (`axis-mcp-probe.sh local|remote`) and fail-closed behavior on probe failure.
- 2026-03-03: Hardened absorbed `projects/SWD-ARC` structure by removing redundant legacy `swd-core/swd-pulse` monolith remnants and nested git metadata, fixing `mcp/server` auth loader parsing/runtime (`SWD_API_KEYS` support + static imports), and adding deterministic absorbed-module verification contract (`projects/SWD-ARC/scripts/verify-absorbed.sh`, `projects/SWD-ARC/docs/ABSORBED_PROJECTS_AUDIT.md`).
- 2026-03-03: Completed SWD-ARC absorption sweep by folding `projects/swd-core` into `projects/SWD-ARC/swd-core`, relocating standalone `projects/swd-os-governance` and `projects/SWD-mcp-server` to archive quarantine, and aligning ARC runtime references to in-repo absorbed paths.
- 2026-03-03: Consolidated workspace project layout by promoting unique archived projects (`wps-hr-core`, `wps-sif-tool`) into `projects/`, quarantining redundant root/archive duplicates under `projects/_archive/2026-03-03-workspace-reorg/`, and removing generated caches from promoted repos (`node_modules`, `.next`, `dist`, `output`, `test-results`).
- 2026-03-01: Enforced mandatory checkout email for paid checkout bootstrap by removing anonymous email fallback in `docsmith-licensing-service` (`/v1/checkout/session`) and requiring valid `customerEmail` before gateway intent creation; aligned `buy.docsmith.tools/buy/manual` instant Ziina flow to block progression until a valid work email is provided.
- 2026-02-26: Applied AXIS directives `DOCSMITH-WEB-RESTRUCTURE-20260226-AXIS01-B`, `DOCSMITH-WEB-THEME-ORGANIC-20260226-AXIS02`, and `DOCSMITH-THEME-LOGO-ALIGN-20260226-AXIS03` by expanding `/docs` into categorized resources/manual/security/routing sections with breadcrumb navigation, FAQ-schema resource pages, and a logo-derived brand-token theme using DocSmith gradient colors.
- 2026-02-26: Applied AXIS addendum `DOCSMITH-WEB-RESTRUCTURE-20260226-AXIS01-A` to `projects/swd-docsmith_brand-website` by adding parent-brand hierarchy in header/footer, constraining footer links to compliance set, converting support content to accordion-based operational sections, and rewriting privacy content into concise offline-first compliance disclosure.
- 2026-02-26: Replaced `projects/swd-docsmith_brand-website` marketing surface with a constrained 5-page gateway (`/`, `/product`, `/pricing`, `/docs`, `/support`), added reusable video/carousel components, added an HTML sitemap page (`/site-map`), and redirected legacy marketing/technical routes to preserve SEO continuity while removing navigation bloat.
- 2026-02-25: `projects/SWD-dev-ec` v0.2 packaging baseline switched runtime persistence to SQLite with auto-migrate, desktop launcher as primary entrypoint, bundled WebView2 dependency bootstrap, and writable data-root fallback (`CommonApplicationData` preferred, user-local fallback) to prevent startup failures on restricted hosts.
- 2026-02-25: Added canonical `docs/SOFTWARE_DEVELOPMENT_WORKFLOW_SOP.md` defining deterministic P0-P5 delivery schema, fail-closed gates, DoR contract, evidence bundle schema, and no-back-and-forth control rules for software development execution.
- 2026-02-06: Established governance docs, skills baseline, and stricter ignore rules. Keeps changes small and audit friendly.
- 2026-02-19: Standardized `social-marketing-content-studio` as a deterministic v1.0 pipeline with required inputs, QA gates, and platform export contract.
- 2026-02-19: Upgraded `operations-control-plane` to governance-grade deterministic flow with context snapshot, housekeeping blocks, telemetry triage, Warden logs, and release-check mode.
- 2026-02-19: Upgraded `testing-governance-suite` to deterministic v1.0 test-pyramid execution with strict stop-on-blocker behavior, governance/SOP/env/state gates, mode contracts, and DocSmith golden pack gate.
- 2026-02-19: Added `vault/` local-first append-only infrastructure (SQLite WAL + migration-driven schema + CLI + local War Room dashboard shell) with DB/artifact paths excluded from git.
- 2026-02-19: Extended Vault to Phase 2 execution cockpit primitives: `swd-vault run`, `swd-vault sweep`, fingerprinted non-info events, and War Room pages for runs, decisions, blockers, and project health.
- 2026-02-19: Added Phase 3 historical ingestion primitives: repo-allowlisted bootstrap doc/log backfill, git commit backfill with dedupe/risk tags, and contextpack sections for recent commits/key docs/recurring failures.
- 2026-02-19: Added Phase 4 profile-based `run --all` orchestration from repo config commands with strict validation, stop-on-failure default, optional continue-on-fail, and summary `run_summary` events.
- 2026-02-19: Added Phase 4.5 War Room control route (`/run-profiles`) plus local-only execution API and event detail pages, enabling safe profile-triggered runs with links to summary/run evidence.
- 2026-02-19: Hardened War Room profile triggers with optional `VAULT_RUN_TOKEN` enforcement on `/api/run-profiles` and moved UI execution to a server action so secrets are not exposed client-side.
- 2026-02-19: Activated Phase 6A/6B vault hardening with append-time hash chaining + `verify chain` integrity checks, status-page chain health indicator, and encrypted backup snapshots via `swd-vault backup --encrypt`.
- 2026-02-19: Added Phase 6C backup operations: retention rotation (`--rotate`, 7/4/6 policy) and restore drill commands (`restore --dry-run` / `--to`) with chain verification and overwrite guardrails.
- 2026-02-19: Added Phase 5A Notion export ingestion (`ingest notion`) with markdown-first default, optional CSV task ingest, dedupe by source path/content hash, redacted excerpts, and `ingest_summary` audit events.
- 2026-02-20: Added Phase 5B one-command Notion zip snapshot ingest (`ingest notion --zip --bundle`) with deterministic extraction, nested zip handling, bundle-dir overwrite guardrails (`--force`), and provenance-rich summary events.
- 2026-02-20: Hardened Notion ingest duplicate controls to reject already-ingested zip bundles pre-extraction (`zip_sha256`), fingerprint markdown/CSV records before insert, and suppress no-op summary events on fully duplicate runs.
- 2026-02-20: Extended Notion ingestion to support older HTML exports via `--include-html`, storing metadata/excerpts only with fingerprint dedupe and no-op summary suppression.
- 2026-02-20: Introduced Phase 7 operational hardening baseline: deterministic event taxonomy stamping (`details_json.event_class`), normalized severity model (`info|notice|warning|critical|fatal` with legacy aliases), verify/seal audit events, and War Room Vault Health/critical-ingest panels.
- 2026-02-20: Extended War Room ingest observability with latest-ingest format coverage ratios, duplicate/anomaly badges, and last-10 ingest state columns (bundle, inserted count, duplicate-only marker), using monotonic `id` ordering for latest selection.
- 2026-02-20: Added ingest provenance fields to Notion `ingest_summary` (`run_profile`, `cli_version`, `host_fingerprint`) and a non-blocking counter-contract warning event for inconsistent ingest counters.
- 2026-02-20: Exposed ingest provenance in War Room status via optional details toggles for latest ingest and last-10 ingests, showing run profile, CLI version, and shortened host fingerprint.
- 2026-02-20: Added minimal forensic provenance toggles to `/timeline` for `ingest_summary`, `backup`, and `restore` rows without adding new list columns.
- 2026-02-20: Froze Vault v1.0 baseline and added canonical governance artifacts (`docs/VAULT_V1_FREEZE_CHECKLIST.md`, `docs/PULSE_PHASE1_READONLY_CONNECTOR_CONTRACT.md`) plus War Room footer build markers (`VAULT_SCHEMA_VERSION`, `VAULT_BUILD_ID`).
- 2026-02-20: Pinned War Room runtime to Node 18 (`vault/dashboard/.nvmrc`, `engines.node`) and documented WSL native rebuild flow for `better-sqlite3` ABI alignment to keep dashboard runtime deterministic.
- 2026-02-20: Published `docs/REPO_OVERLAP_INTELLIGENCE_PACK_V1.md` and `docs/SWD_LOG_BUNDLE_SPEC_V1.md` as canonical inputs for consolidation planning and NDJSON-based governance-log ingestion.
- 2026-02-20: Triaged `npm audit` for `vault/dashboard`: upgraded `next` from `14.2.24` to `14.2.35` to remove critical advisories; residual high advisory (`GHSA-9g9p-9gw9-jx7f`, `GHSA-h25m-26qc-wcjf`) requires breaking major upgrade and is accepted short-term due localhost-only War Room scope, with remediation trigger set to next planned framework-major cycle.
- 2026-02-20: Enforced local-first connector policy by removing cloud-connector references from active docs/specs, removing connector checkup execution from root `package.json`, deleting connector-specific repo/skill assets from this workspace, and introducing a single deferred connector marker in `docs/ROADMAP_CONNECTORS.md`.
- 2026-02-20: Added canonical operator artifact `docs/SWD_OS_OPERATOR_MANUAL.md` with local-first runtime procedures, integrity/backup/restore playbooks, anomaly handling, and PDF export contract aligned to Vault v1.0 freeze.
- 2026-02-20: Upgraded `docs/SWD_OS_OPERATOR_MANUAL.md` to enterprise audit-grade format with document control metadata, controlled distribution policy, revision history table, and formal approval/sign-off block.
- 2026-02-20: Added runtime divergence guardrails by standardizing workspace pinning to Node `20.11.1` (`.nvmrc`, `vault/dashboard/.nvmrc`, dashboard `engines.node >=20.9.0`), adding `checkup:runtime` gate (`tools/check-runtime-contract.sh`), and publishing canonical policy/contract docs (`docs/WORKSPACE_RUNTIME_GOVERNANCE_POLICY.md`, `docs/ENVIRONMENT_CONTRACT.md`).
- 2026-02-20: Implemented Phase 1 executable `swd-vault ingest logs --bundle ...` based on `docs/SWD_LOG_BUNDLE_SPEC_V1.md`, including deterministic canonicalization, NDJSON bundle artifacts, bundle/file/record duplicate rejection, and ingest-summary/no-op suppression semantics.
- 2026-02-20: Standardized PDF artifact hosting to canonical non-versioned filenames, removed legacy versioned whitepaper outputs (`DocSmith-Security-Whitepaper-v1.pdf`), regenerated current website PDFs, and added cache-busted website doc links via `src/lib/docsAssets.js` to prevent stale manual downloads.
- 2026-02-20: Reframed `docs/SWD_OS_OPERATOR_MANUAL.md` into a UI/UX-first operator manual focused on War Room screen playbooks, operator journeys, interaction semantics, and failure-state handling, with commands moved to a compact technical appendix.
- 2026-02-20: Rescoped External Forge governance pack as external-production-environment SOP only (not ARC-internal member SOP), updated operator/Vault labels and subject conventions, and kept mirrored governance guardrails in `~/.codex/config.toml`.
- 2026-02-23: Consolidated `projects/SWD-mcp-server` runtime authority to `src/server.js` with a compatibility root shim, restored Vault query allowlist enforcement for list/search, and hardened MCP auth/CORS/rate-limit controls while preserving the frozen tool surface.
- 2026-02-23: Added `projects/swd-docsmith_brand-website` route `/cfo-workspace` as an operational UAE SME finance MVP (runway/burn/break-even/VAT due estimator + board snapshot export), linked in primary navigation and sitemap to keep `docsmith.tools` as the product execution surface while preserving no-public-feed risk posture.
- 2026-02-24: Added `app/api/cfo/licensing` server proxy + normalized entitlement helpers for `projects/swd-docsmith_brand-website`, gated CFO export actions to active paid plans, and captured inbound attribution query params for campaign source tracking without introducing public social feed behavior.
